# Public backend URL only. Convex dev prints your development deployment URL.
VITE_CONVEX_URL=
# Set FIRECRAWL_API_KEY and ADMIN_REVIEW_TOKEN in the Convex dashboard,
# never in a VITE_ variable. Enable discovery there with DISCOVERY_ENABLED=true.
