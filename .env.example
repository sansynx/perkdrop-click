# Public backend URL only. Convex dev prints your development deployment URL.
VITE_CONVEX_URL=
# Set these in the Convex dashboard, never in a VITE_ variable:
# FIRECRAWL_API_KEY, ADMIN_REVIEW_TOKEN, DISCOVERY_ENABLED,
# AGENTMAIL_API_KEY, AGENTMAIL_WEBHOOK_SECRET, AGENTMAIL_INTAKE_ADDRESS
# PUBLIC_SITE_URL must match the live app URL in README (Visit Perkdrop).
# CONVEX_SITE_URL is set by Convex. Receipts load /brand/mark.png from it.
# Webhook path: /agentmail/webhook
